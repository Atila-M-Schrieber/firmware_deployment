# This should be the same for most deployed boards.

firmware = "blinker"
version = "0.1.1" # Not generally appropriate for a config file
firmware_url = "http://192.168.1.209:8000/firmware"
blink_frequency = 1 # in Hertz

# How frequently the server is contacted, in seconds - low for development
polling_rate = 10
